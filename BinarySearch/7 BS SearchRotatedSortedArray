In this question we need to find element in sorted rotated array

First find the index of smallest element
It will divide array into two sorted array
Apply Binary Search in both arrays

int start=0;
int end=arr.length-1;
int index=getIndex(arr);
int res1 = BinarySearch(arr,start,index-1);
int res2 = BinarySearch(arr,index,end);
if(res1==-1 && res2==-1) return -1;
return Math.Max(res1,res2);


private int getIndex(int arr[]){
int n = arr.length;
while(start<=end)
{
int mid=start + (end-start)/2;
int next=(mid+1)%n;
int prev=(mid+n-1)%n;
//Index of minimum element gives the number of times the array is rotated.
if(arr[mid]<=arr[next]&&arr[mid]<=arr[prev]) return mid; //to get minimum element 
if(arr[start]<=arr[mid]) start=mid+1; //Explore towards unsorted array 
else if(arr[mid]<=arr[end]) end=mid-1; //Explore towards unsorted array
}
}

private int BinarySearch(int[], int start, int end){
    while(start<end){
    if(ele == arr[mid]) return mid;
    else if(ele < arr[mid]) end=mid-1;
    else if(ele > arr[mid]) start=mid+1;
} 
return -1;
}