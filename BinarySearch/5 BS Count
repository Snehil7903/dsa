given arr[] and element ele
the element will definitely be consecutive as it is a sorted array.
so we will get count by lastoccur-firstoccur+1

int first=FirstOccur(arr,ele);
int last=LastOccur(arr,ele);
return last-first+1;

private int FirstOccur(int []arr,int ele){
    int size= arr.length;
    int start = 0;
    int end= size-1;
    int result=-1;
    while(start<end){
    if(ele == arr[mid]){
        //we will not return rather search in left for first occur
        res=mid;
        end=mid-1;
    }
    else if(ele < arr[mid]) end=mid-1;
    else if(ele > arr[mid]) start=mid+1;
}
return res;
}

private int LastOccur(int[]arr,int ele){
    int size= arr.length;
    int start = 0;
    int end= size-1;
    int result=-1;
    while(start<end){
    if(ele == arr[mid]){
        //we will not return rather search in right for last occur
        res=mid;
        start=mid+1;
    }
    else if(ele < arr[mid]) end=mid-1;
    else if(ele > arr[mid]) start=mid+1;
}
return res;
}