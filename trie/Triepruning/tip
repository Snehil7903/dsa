"Not necessarily. For Trie-II style implementations, I would simply decrement prefixCount and endCount. 
Nodes are kept because they may still be shared by other words. 
Physical node deletion is an optional optimization that can be done only when a node's prefixCount becomes 0 and it has no children."