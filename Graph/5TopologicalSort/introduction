The Key Insight

A node should be added after all its dependencies are processed.

for(neighbor)
    dfs(neighbor);

stack.push(node);

Topological Sort exists iff the graph is a DAG

(DAG = Directed Acyclic Graph)

Cycle ⇒ No Topological Ordering